import 'package:flutter/material.dart';

class HeartView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Heart'),
      ),
      body: Center(
        child: Text('This is the Heart page'),
      ),
    );
  }
}