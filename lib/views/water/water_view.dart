import 'package:flutter/material.dart';

class WaterView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Water'),
      ),
      body: Center(
        child: Text('This is the Water page'),
      ),
    );
  }
}